# Comma-separated browser origins allowed to call the API.
CORS_ORIGINS=http://localhost:5173,https://campus-loop.pages.dev

# Defaults to api/data/campus_loop.db when omitted.
# DATABASE_URL=sqlite:////absolute/path/to/campus_loop.db
